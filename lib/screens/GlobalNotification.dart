import 'package:flutter/material.dart';

class GlobalNotification extends StatelessWidget {
  const GlobalNotification({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("mere dil me jagah")),
    );
  }
}
